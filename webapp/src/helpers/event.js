
function formatPrice(price) {
  return `${price}円`
}

function formatOpenDate(date) {
  return `${date}`
}

function formatRegistrationDateTime(datetime) {
  return `${datetime}`
}

module.exports = {
  formatPrice,
  formatOpenDate,
  formatRegistrationDateTime,
}
