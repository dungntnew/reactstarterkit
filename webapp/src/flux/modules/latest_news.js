import {initLatestNews as initialState} from './initialState'

export const fetchLatestNewsIfNeed = (limit) => {
	return (dispatch) => {

	}
}

const latestNewsReducer = (state = initialState, action) => {
	return state
}

export default latestNewsReducer;